<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:controls="clr-namespace:Ovotan.Windows.Controls;assembly=Ovotan.Windows.Controls"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--<Style x:Key="{x:Static ToolBar.ButtonStyleKey}"
       BasedOn="{StaticResource FFFFFFF}"
       TargetType="{x:Type controls:ViewboxButton}" />-->

    
    <Style x:Key="{x:Type ToolBar}" TargetType="{x:Type ToolBar}">
        <Style.Resources>
            <SolidColorBrush x:Key="ToolBarBackground">#CCD5F0</SolidColorBrush>
            <SolidColorBrush x:Key="ToggleButtonMouseOverBackground">#F6FBFF</SolidColorBrush>
            <SolidColorBrush x:Key="ToggleButtonArrowFill">Black</SolidColorBrush>
            <SolidColorBrush x:Key="ToggleButtonListBorder">#94A6CA</SolidColorBrush>
            <SolidColorBrush x:Key="ToggleButtonListBackground">#E9EEFF</SolidColorBrush>
            <Style TargetType="{x:Type controls:ViewboxButton}">
                <Style.Resources>
                    <SolidColorBrush x:Key="BorderBrushMouseOver">#825D07</SolidColorBrush>
                    <SolidColorBrush x:Key="BackgroundMouseOver">#FFEDC8</SolidColorBrush>
                    <Color x:Key="GreenColor">#006758</Color>
                    <Color x:Key="DarkYellow">#996f00</Color>
                </Style.Resources>
                <Setter Property="Margin" Value="0,0,1,0"/>
                <Setter Property="OverridesDefaultStyle" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate>
                            <Border x:Name="Border" Padding="2"  BorderBrush="Transparent" BorderThickness="1">
                                <ContentControl x:Name="MainViewBox"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource BorderBrushMouseOver}"/>
                                    <Setter TargetName="Border" Property="Background" Value="{StaticResource BackgroundMouseOver}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Style.Resources>
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="Background" Value="AntiqueWhite"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToolBar}">
                    <Border x:Name="Border" Background="{StaticResource ToolBarBackground}">
                        <DockPanel>
                            <ToggleButton DockPanel.Dock="Right"
                                IsEnabled="{TemplateBinding HasOverflowItems}"
                                ClickMode="Press"
                                IsChecked="{Binding IsOverflowOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                                        <Border x:Name="Border" CornerRadius="0,3,3,0" SnapsToDevicePixels="true" Background="Transparent">
                                            <Grid>
                                                <Path x:Name="Arrow" Fill="{StaticResource ToggleButtonArrowFill}" VerticalAlignment="Bottom" Margin="2,3" Data="M -0.5 3 L 5.5 3 L 2.5 6 Z" />
                                                <ContentPresenter />
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource ToggleButtonMouseOverBackground}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                                <Popup x:Name="OverflowPopup"
                                    AllowsTransparency="true"
                                    Placement="Bottom"
                                    StaysOpen="false"
                                    Focusable="false"
                                    PopupAnimation="Slide"
                                    IsOpen="{Binding IsOverflowOpen, 
                                    RelativeSource={RelativeSource TemplatedParent}}">
                                    <Border x:Name="DropDownBorder" BorderThickness="1" 
                                            BorderBrush="{StaticResource ToggleButtonListBorder}"
                                            Background="{StaticResource ToggleButtonListBackground}">
                                        <ToolBarOverflowPanel x:Name="PART_ToolBarOverflowPanel"
                                        Margin="2"
                                        WrapWidth="200"
                                        Focusable="true"
                                        FocusVisualStyle="{x:Null}"
                                        KeyboardNavigation.TabNavigation="Cycle"
                                        KeyboardNavigation.DirectionalNavigation="Cycle" />
                                    </Border>
                                </Popup>
                                <ToggleButton.Triggers>
                                </ToggleButton.Triggers>
                            </ToggleButton>
                            <ToolBarPanel x:Name="PART_ToolBarPanel" IsItemsHost="true" Margin="2,2,2,2" />
                        </DockPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


</ResourceDictionary>